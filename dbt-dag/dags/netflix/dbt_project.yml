name: 'netflix'
version: '1.0.0'

profile: 'netflix'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  netflix:
    +materialized: view
    dim:
      +materialized: table 
    fct:
      +materialized: table
    mart:
      +materialized: table
  
